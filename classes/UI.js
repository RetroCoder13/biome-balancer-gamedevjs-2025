class UI{
    constructor(){
        this.active = false
    }

    setActive(state){
        this.active = state
    }

    update(renderManager,inputManager){
        if(this.active){

        }
    }
}

class MapUI extends UI{
    update(renderManager,inputManager){
        if(this.active){

        }
    }
}

class CompendiumUI extends UI{
    update(renderManager,inputManager){
        if(this.active){
            
        }
    }
}